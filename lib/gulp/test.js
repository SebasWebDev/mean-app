var gulp = require('gulp'),
	config = require('./config.js');

// gulp.task('unit-test', function() {
//     return gulp.src(config.filesets.test)
//         // gulp-ava needs filepaths so you can't have any plugins before it
//         .pipe(ava());
// });